import Config from "./interfaces/Config";
import configColors from "./config/colors";
import configPartTypes from "./config/partTypes";
import configParts from "./config/parts";

const config: Config = {
  colors: configColors,
  partTypes: configPartTypes,
  parts: configParts
};

export default config;
